import { projectSchema } from './schemas/project';
import { aboutSchema } from './schemas/about';
import { journalSchema } from './schemas/journal';

export const schemaTypes = [projectSchema, aboutSchema, journalSchema];
